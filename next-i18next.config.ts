import type { UserConfig } from 'next-i18next';

const config: UserConfig = {
  i18n: {
    defaultLocale: 'tr',
    locales: ['tr', 'en', 'es', 'it', 'sr', 'ro', 'bs', 'ru'],
  },
};

export default config;
